{
  "name": "openspeak",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch" : "npx watchify -v --exclude *.json ./src/bin/*.js -o ./public/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.1.3",
    "ipfs-core": "^0.14.1",
    "web3": "^1.7.0"
  },
  "devDependencies": {
    "watchify": "^4.0.0"
  }
}
